GROQ_API_KEY=
ANTHROPIC_API_KEY=
PORT=8787
# Optional — enables the shared global style library. Railway's Postgres plugin
# injects this automatically; leave blank for local dev (that feature just disables).
DATABASE_URL=
